SRC = FileKeeperTest.cpp \
		FileKeeper.cpp

OBJ = $(SRC:.cpp=.o)
CFLAGS = -std=c++11 -O3 -v

.cpp.o:
	g++ -c $< $(CFLAGS)

all: test

test: $(OBJ)
	g++ $(OBJ) -o filekeepertest $(CFLAGS)

clean:
	rm -f *.o filekeepertest
